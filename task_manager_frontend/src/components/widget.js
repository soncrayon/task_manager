import React from 'react'

export const Widget = (props) => {
    return (
        <div className="widget">
            <h4>Replace this with widget title from props</h4>
            <p>Victory graph component goes here</p>
            <p>and here is the legend if not already included in victory graph</p>
        </div>
    )
}