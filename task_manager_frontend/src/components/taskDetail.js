import React from 'react'
import { Notes } from './notes'

export const taskDetail = (props) => {
    return(
        <div>
            {/* include task data and description here */}
            {/* build out notes component below on front and backend */}
            <Notes /> 
        </div>
    )
}